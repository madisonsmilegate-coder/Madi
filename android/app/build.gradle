plugins { id 'com.android.application' }

android {
    namespace 'com.miccheck.battles'
    compileSdk 34

    defaultConfig {
        applicationId "com.miccheck.battles"
        minSdk 24                 // Android 7.0; WebView features used need nothing newer
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false   // the app is one HTML asset; nothing to shrink
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.webkit:webkit:1.11.0'
}
